# health_assessment package

"""Utilities for generating a health‑assessment report.

- `doc_audit.py` gathers README and documentation files.
- `structure_report.py` builds a language‑wise file count and a simple dependency graph.
- `report_generator.py` combines the outputs into a Markdown/HTML report and writes `health_report.md`.
"""
